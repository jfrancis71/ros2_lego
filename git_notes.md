Just some personal notes to help me understand git

git pull:
```
git fetch
git merge
```

git clone:
```
mkdir dir
git init ??? Won't we now have two roots??
git remote add
git fetch
git checkout
```
